// ccwrap watcom: <climits>. Forward to Open Watcom's native <climits>, then add the
// C23 / C++20 *_WIDTH macros (bit-widths of the integer types) and the other C23
// additions, which Open Watcom's headers predate.
#pragma once

#include <ccwrap_common.h>
#include _CCW_NATIVE_C_HEADER_PATH(climits)
#include "../../detail/c/limits_c23.h"
