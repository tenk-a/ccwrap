// ccwrap watcom forwarder: pull the genuine C++ <filesystem> from detail/llibcxx03/include so that
// putting only watcom/ on the include path overrides Open Watcom's native header.
#pragma once
#include <ccwrap_common.h>

#if !defined(_CCW_OS_WIN) && !defined(_CCW_OS_DOS)
# error "ccwrap: <filesystem> has an OS layer for Win32 and DOS only on Open Watcom."
#endif

#include _CCW_NATIVE_STL_HEADER_PATH(filesystem)
