// ccwrap watcom: <cassert>. Thin forwarder to Open Watcom's native <cassert> (which
// defines the assert() macro, honouring NDEBUG), guaranteeing ccwrap_common.h is
// pulled in first. No supplements are needed.
#pragma once

#include <ccwrap_common.h>
#include _CCW_NATIVE_C_HEADER_PATH(cassert)
