// ccwrap watcom forwarder: pull the genuine C++ <debugging> from detail/llibcxx03/include so that
// putting only watcom/ on the include path overrides Open Watcom's native header.
#pragma once
#include <ccwrap_common.h>

#if !defined(_CCW_OS_WIN)
# error "ccwrap: <debugging> is Win32-only on Open Watcom -- std::is_debugger_present rests on Win32 IsDebuggerPresent."
#endif
#include _CCW_NATIVE_STL_HEADER_PATH(debugging)
