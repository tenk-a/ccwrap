// ccwrap watcom: <cctype>. Thin forwarder to Open Watcom's native <cctype> (which
// declares the C character-classification functions in namespace std), guaranteeing
// ccwrap_common.h is pulled in first. No supplements are needed yet.
#pragma once

#include <ccwrap_common.h>
#include _CCW_NATIVE_C_HEADER_PATH(cctype)
