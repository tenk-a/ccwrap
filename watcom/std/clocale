// ccwrap watcom: <clocale>. The C locale layer lives in <locale.h> (ccwrap's
// own, not the native one); this pulls it in and takes the std names from it.
#pragma once

#include <ccwrap_common.h>
#include <locale.h>
